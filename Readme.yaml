# Take it as a life's work to do!
# Java
Java:
   version: 1.8

# Gradle
Gradle: 2.9

# Spring
Spring:
   Spring Framework: 4.2.5
   Spring Boot + Gradle: 1.3.3

# Mysql
Mysql:
   5.7 windows
   my.default.ini改名为my.ini放到bin目录
   mysqld --initialize --user=mysql --console
   记下临时密码
   启动服务.
   mysql -uroot -p **
   set password = password('root')


# Rabbitmq
Rabbitmq:
   full-name: rabbitmq-server-3.6.1
   rabbitmqctl  add_user  zhangwei  zhangwei
   rabbitmqctl  set-permissions -p / zhangwei .* .* .*

# Neo4j [optional: rest]
Neo4j:

